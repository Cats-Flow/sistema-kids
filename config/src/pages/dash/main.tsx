import React from 'react'
import ReactDOM from 'react-dom/client'
import { Dash } from '.'

ReactDOM.createRoot(document.getElementById('dash')!).render(
  <React.StrictMode>
    <Dash />
  </React.StrictMode>
)
