import React from 'react'
import ReactDOM from 'react-dom/client'
import { Juniores } from '.'

ReactDOM.createRoot(document.getElementById('juniores')!).render(
  <React.StrictMode>
    <Juniores />
  </React.StrictMode>
)
