import React from 'react'
import ReactDOM from 'react-dom/client'
import { Maternal } from '.'

ReactDOM.createRoot(document.getElementById('maternal')!).render(
  <React.StrictMode>
    <Maternal />
  </React.StrictMode>
)
