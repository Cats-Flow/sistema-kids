import React from 'react'
import ReactDOM from 'react-dom/client'
import { Aulas } from '.'

ReactDOM.createRoot(document.getElementById('geaula')!).render(
  <React.StrictMode>
    <Aulas />
  </React.StrictMode>
)
